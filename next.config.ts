import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['images.unsplash.com', 'ecoplastinteriors.s3.eu-north-1.amazonaws.com', 'realplast.in', 'www.zmartbuild.com', 'homerenot.com', 'blueskywindows.com.au', 'plasticranger.com'],
  },
}

export default nextConfig;
