import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['images.unsplash.com', 'ecoplastinteriors.s3.eu-north-1.amazonaws.com'],
  },
}

export default nextConfig;
